{
  "$comment": [
    "Input to `bun run grammars:update`, which wraps OpenTUI's own update-assets script.",
    "Grammar and queries come from the SAME published package version so they cannot drift: a query",
    "written against a newer grammar fails to compile and the language silently loses its parser.",
    "Measured: nvim-treesitter's python query against tree-sitter-python 0.25.0 does exactly that.",
    "Only .wasm builds shipped by the grammar's own maintainers are used \u2014 tree-sitter-wasms@0.1.13",
    "is built with the 0.20 CLI and its yaml grammar imports symbols web-tree-sitter 0.25 does not",
    "export, which crashes the parser worker mid-parse rather than failing to load.",
    "SQL is the one exception, and a `wasm` here may be a LOCAL PATH for exactly that case: nobody",
    "publishes a SQL wasm, so the committed one was compiled with the CLI pinned to the same",
    "web-tree-sitter the runtime bundles \u2014 which is what the maintainer-built rule was protecting.",
    "Adding a language is one entry here plus `bun run grammars:update`; the wasm is the cost",
    "(~0.2-1.1MB, and 2.3MB for SQL and 3.3MB for C++, by far the largest assets here)."
  ],
  "parsers": [
    {
      "filetype": "python",
      "wasm": "https://cdn.jsdelivr.net/npm/tree-sitter-python@0.25.0/tree-sitter-python.wasm",
      "queries": {
        "highlights": [
          "https://cdn.jsdelivr.net/npm/tree-sitter-python@0.25.0/queries/highlights.scm"
        ]
      }
    },
    {
      "filetype": "bash",
      "aliases": [
        "shell"
      ],
      "wasm": "https://cdn.jsdelivr.net/npm/tree-sitter-bash@0.25.1/tree-sitter-bash.wasm",
      "queries": {
        "highlights": [
          "https://cdn.jsdelivr.net/npm/tree-sitter-bash@0.25.1/queries/highlights.scm"
        ]
      }
    },
    {
      "filetype": "dockerfile",
      "aliases": [
        "docker"
      ],
      "$comment": "Built locally rather than downloaded — no dockerfile wasm is published: npm's tree-sitter-dockerfile is a 0.0.1-security placeholder and the grammar ships no release artefacts. The build script is apps/tui/scripts/build-dockerfile-grammar.ts; it needs Docker and only runs when the wasm is missing, which for a normal checkout is never. The query is pinned to the same grammar tag the wasm was built from, per the same-version rule above.",
      "wasm": "src/ui/markdown/grammars/vendor/tree-sitter-dockerfile.wasm",
      "queries": {
        "highlights": [
          "https://cdn.jsdelivr.net/gh/camdencheek/tree-sitter-dockerfile@v0.2.0/queries/highlights.scm"
        ]
      }
    },
    {
      "filetype": "json",
      "wasm": "https://cdn.jsdelivr.net/npm/tree-sitter-json@0.24.8/tree-sitter-json.wasm",
      "queries": {
        "highlights": [
          "https://cdn.jsdelivr.net/npm/tree-sitter-json@0.24.8/queries/highlights.scm"
        ]
      }
    },
    {
      "filetype": "css",
      "wasm": "https://cdn.jsdelivr.net/npm/tree-sitter-css@0.25.0/tree-sitter-css.wasm",
      "queries": {
        "highlights": [
          "https://cdn.jsdelivr.net/npm/tree-sitter-css@0.25.0/queries/highlights.scm"
        ]
      }
    },
    {
      "filetype": "sql",
      "$comment": "Built locally rather than downloaded \u2014 no SQL wasm is published. The build script that produced this artefact is deprecated/tui/scripts/build-sql-grammar.ts; it needs Docker and only runs when the wasm is missing, which for a normal checkout is never.",
      "wasm": "src/ui/markdown/grammars/vendor/tree-sitter-sql.wasm",
      "queries": {
        "highlights": [
          "https://cdn.jsdelivr.net/npm/@derekstride/tree-sitter-sql@0.3.11/queries/highlights.scm"
        ]
      }
    },
    {
      "filetype": "yaml",
      "wasm": "https://cdn.jsdelivr.net/npm/@tree-sitter-grammars/tree-sitter-yaml@0.7.1/tree-sitter-yaml.wasm",
      "queries": {
        "highlights": [
          "https://cdn.jsdelivr.net/npm/@tree-sitter-grammars/tree-sitter-yaml@0.7.1/queries/highlights.scm"
        ]
      }
    },
    {
      "filetype": "java",
      "wasm": "https://cdn.jsdelivr.net/npm/tree-sitter-java@0.23.5/tree-sitter-java.wasm",
      "queries": {
        "highlights": [
          "https://cdn.jsdelivr.net/npm/tree-sitter-java@0.23.5/queries/highlights.scm"
        ]
      }
    },
    {
      "filetype": "go",
      "wasm": "https://cdn.jsdelivr.net/npm/tree-sitter-go@0.25.0/tree-sitter-go.wasm",
      "queries": {
        "highlights": [
          "https://cdn.jsdelivr.net/npm/tree-sitter-go@0.25.0/queries/highlights.scm"
        ]
      },
      "aliases": [
        "golang"
      ]
    },
    {
      "filetype": "rust",
      "wasm": "https://cdn.jsdelivr.net/npm/tree-sitter-rust@0.24.0/tree-sitter-rust.wasm",
      "queries": {
        "highlights": [
          "https://cdn.jsdelivr.net/npm/tree-sitter-rust@0.24.0/queries/highlights.scm"
        ]
      }
    },
    {
      "filetype": "c",
      "aliases": [
        "h"
      ],
      "$comment": "The cpp entry already vendors this query as its base layer; this is the same query against C's own, much smaller grammar. Its first pattern is `(identifier) @variable`, so every name is coloured \u2014 that is upstream's choice, not a misconfiguration.",
      "wasm": "https://cdn.jsdelivr.net/npm/tree-sitter-c@0.24.1/tree-sitter-c.wasm",
      "queries": {
        "highlights": [
          "https://cdn.jsdelivr.net/npm/tree-sitter-c@0.24.1/queries/highlights.scm"
        ]
      }
    },
    {
      "filetype": "cpp",
      "aliases": [
        "c++"
      ],
      "wasm": "https://cdn.jsdelivr.net/npm/tree-sitter-cpp@0.23.4/tree-sitter-cpp.wasm",
      "queries": {
        "highlights": [
          "https://cdn.jsdelivr.net/npm/tree-sitter-c@0.24.1/queries/highlights.scm",
          "https://cdn.jsdelivr.net/npm/tree-sitter-cpp@0.23.4/queries/highlights.scm"
        ]
      },
      "$comment": "C++'s published highlights.scm is a DELTA on tree-sitter-c's \u2014 alone it colours nothing but C++-only syntax. nvim-treesitter expresses this as `; inherits: c`; here the base query is listed first and the generator concatenates them. This is the one place grammar and query versions deliberately differ, because the C++ grammar is a superset of C's node types."
    },
    {
      "filetype": "javascript",
      "aliases": [
        "javascriptreact",
        "jsx"
      ],
      "$comment": "One grammar for both: tree-sitter-javascript parses JSX natively, so `jsx` is the same parser with the JSX query appended. The query is inert on plain JS \u2014 no jsx nodes, no matches. Registering `javascript` here REPLACES the parser OpenTUI bundles, whose query gates two `(identifier)` patterns behind nvim's `#lua-match?`; the worker files unknown predicates as non-text predicates and never evaluates them, so every identifier matched @constant and rendered blue.",
      "wasm": "https://cdn.jsdelivr.net/npm/tree-sitter-javascript@0.25.0/tree-sitter-javascript.wasm",
      "queries": {
        "highlights": [
          "https://cdn.jsdelivr.net/npm/tree-sitter-javascript@0.25.0/queries/highlights.scm",
          "https://cdn.jsdelivr.net/npm/tree-sitter-javascript@0.25.0/queries/highlights-jsx.scm"
        ]
      }
    },
    {
      "filetype": "typescript",
      "$comment": "Layered like cpp: this grammar's published highlights.scm is a DELTA and colours nothing on its own. nvim-treesitter writes `; inherits: javascript`; here the base queries are listed first and the generator concatenates them.",
      "wasm": "https://cdn.jsdelivr.net/npm/tree-sitter-typescript@0.23.2/tree-sitter-typescript.wasm",
      "queries": {
        "highlights": [
          "https://cdn.jsdelivr.net/npm/tree-sitter-javascript@0.25.0/queries/highlights.scm",
          "https://cdn.jsdelivr.net/npm/tree-sitter-typescript@0.23.2/queries/highlights.scm"
        ]
      }
    },
    {
      "filetype": "typescriptreact",
      "aliases": [
        "tsx"
      ],
      "$comment": "Layered like cpp: this grammar's published highlights.scm is a DELTA and colours nothing on its own. nvim-treesitter writes `; inherits: javascript`; here the base queries are listed first and the generator concatenates them. Its own wasm, not typescript's: the two grammars disagree on `<T>expr`, which is a type assertion in .ts and JSX in .tsx.",
      "wasm": "https://cdn.jsdelivr.net/npm/tree-sitter-typescript@0.23.2/tree-sitter-tsx.wasm",
      "queries": {
        "highlights": [
          "https://cdn.jsdelivr.net/npm/tree-sitter-javascript@0.25.0/queries/highlights.scm",
          "https://cdn.jsdelivr.net/npm/tree-sitter-javascript@0.25.0/queries/highlights-jsx.scm",
          "https://cdn.jsdelivr.net/npm/tree-sitter-typescript@0.23.2/queries/highlights.scm"
        ]
      }
    },
    {
      "filetype": "html",
      "aliases": [
        "htm"
      ],
      "wasm": "https://cdn.jsdelivr.net/npm/tree-sitter-html@0.23.2/tree-sitter-html.wasm",
      "queries": {
        "highlights": [
          "https://cdn.jsdelivr.net/npm/tree-sitter-html@0.23.2/queries/highlights.scm"
        ]
      }
    },
    {
      "filetype": "php",
      "aliases": [
        "php5",
        "php7",
        "php8",
        "phtml"
      ],
      "$comment": "tree-sitter-php ships two grammars: `php` opens in HTML and enters PHP at `<?php`, `php_only` assumes the whole file is PHP. A fenced block is written as a file, opening tag included, so `php` is the one that parses it.",
      "wasm": "https://cdn.jsdelivr.net/npm/tree-sitter-php@0.24.2/tree-sitter-php.wasm",
      "queries": {
        "highlights": [
          "https://cdn.jsdelivr.net/npm/tree-sitter-php@0.24.2/queries/highlights.scm"
        ]
      }
    },
    {
      "filetype": "lua",
      "aliases": [
        "luau"
      ],
      "$comment": "49KB \u2014 an order of magnitude smaller than any other grammar here, because Lua's own is a small language. At that price the parse is strictly better than lexing it.",
      "wasm": "https://cdn.jsdelivr.net/npm/@tree-sitter-grammars/tree-sitter-lua@0.4.1/tree-sitter-lua.wasm",
      "queries": {
        "highlights": [
          "https://cdn.jsdelivr.net/npm/@tree-sitter-grammars/tree-sitter-lua@0.4.1/queries/highlights.scm"
        ]
      }
    },
    {
      "filetype": "toml",
      "aliases": [
        "tml"
      ],
      "$comment": "24KB. The @tree-sitter-grammars build is the one with a wasm; plain tree-sitter-toml publishes queries only.",
      "wasm": "https://cdn.jsdelivr.net/npm/@tree-sitter-grammars/tree-sitter-toml@0.7.0/tree-sitter-toml.wasm",
      "queries": {
        "highlights": [
          "https://cdn.jsdelivr.net/npm/@tree-sitter-grammars/tree-sitter-toml@0.7.0/queries/highlights.scm"
        ]
      }
    }
  ]
}
