{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": ["tsconfig.base.json"],
  "globalPassThroughEnv": ["ATLAS_HOME", "NODE_ENV", "ATLAS_TEST_SHARDS"],
  "tasks": {
    "typecheck": {
      "dependsOn": ["^typecheck", "^build"],
      "outputs": []
    },
    "test": {
      "dependsOn": ["^test"],
      "outputs": []
    },
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["bin/**", "dist/**", "src/generated/**", ".next/**"]
    }
  }
}
