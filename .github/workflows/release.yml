name: release

# Gates on the ci workflow instead of running its own test suite: ci.yml already runs
# typecheck, the sharded TUI suite, the package suites and the binary builds on every
# push to main, so a second serialized copy here only delayed failures (a red run took
# ~18min in the duplicated test step alone). conclusion == success means a release can
# only be cut from a commit whose full main CI is green.
on:
  workflow_run:
    workflows: [ci]
    types: [completed]
    branches: [main]

permissions:
  contents: write
  actions: write

# Supersede, never queue: the version is always computed from everything since the last
# tag, so cancelling an in-flight build loses nothing — its commits ride the next run.
# Without this a merge landing mid-build waited behind a release its own commits make
# stale.
concurrency:
  group: release
  cancel-in-progress: true

jobs:
  release:
    runs-on: ubuntu-latest
    if: github.event.workflow_run.conclusion == 'success'
    env:
      GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      TARGET_SHA: ${{ github.event.workflow_run.head_sha }}
    steps:
      # head_sha, not the default branch ref: GITHUB_SHA/GITHUB_REF under workflow_run
      # point at main's current HEAD, which may be a merge that landed after the ci run
      # this release is gating on.
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.workflow_run.head_sha }}
          fetch-depth: 0

      - uses: oven-sh/setup-bun@v2

      # Cross-compiles three targets from one ubuntu host, and @vscode/ripgrep ships its binaries
      # as os/cpu-gated optional packages — without the overrides only the host's rg is installed
      # and staging the vendored binary for the darwin targets fails.
      - run: bun install --os='*' --cpu='*'

      - name: compute release version
        id: version
        run: bun apps/tui/scripts/release-version.ts >> "$GITHUB_OUTPUT"

      - name: build binaries
        if: steps.version.outputs.skip != 'true'
        working-directory: apps/tui
        env:
          ATLAS_VERSION: ${{ steps.version.outputs.version }}
          ATLAS_RELEASE_REPO: ${{ github.repository }}
          ATLAS_BUILD_SHA: ${{ github.event.workflow_run.head_sha }}
        run: |
          set -euo pipefail
          bun run build:prepare
          for target in bun-darwin-arm64 bun-darwin-x64 bun-linux-x64; do
            asset="atlas-${target#bun-}"
            bun scripts/build.ts --target "$target" --outfile "dist/${asset}"
            shasum -a 256 "dist/${asset}" > "dist/${asset}.sha256"
          done

      - name: create release
        if: steps.version.outputs.skip != 'true'
        working-directory: apps/tui
        env:
          VERSION: ${{ steps.version.outputs.version }}
          PREV_TAG: ${{ steps.version.outputs.prev_tag }}
        run: |
          set -euo pipefail
          # --target pins the tag to the commit these binaries were built from: without it
          # gh release create tags the default branch's HEAD at creation time, and a merge
          # that lands while this run is building would tag a commit past the built one.
          args=(--title "atlas v${VERSION}" --generate-notes --target "$TARGET_SHA")
          if [[ -n "$PREV_TAG" ]]; then
            args+=(--notes-start-tag "$PREV_TAG")
          fi
          gh release create "tui-v${VERSION}" dist/* "${args[@]}"

      - name: dispatch sandbox image retag
        if: steps.version.outputs.skip != 'true'
        env:
          VERSION: ${{ steps.version.outputs.version }}
        run: gh workflow run sandbox-image.yml -f release_tag="tui-v${VERSION}" -f release_sha="$TARGET_SHA"
